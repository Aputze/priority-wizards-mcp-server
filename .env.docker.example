# Host paths for Docker volume mounts (Windows: use forward slashes)
# These are ONLY used by docker-compose.yml — not read by Node inside the container.
WIZ1_HOST_DIR=D:/priority/tmp/chm_extract_wiz1
WIZ3_HOST_DIR=D:/priority/tmp/chm_extract_wiz3

# Published host port
PORT=3040
